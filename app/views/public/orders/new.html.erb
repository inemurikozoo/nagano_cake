<div class="col-md-11 mx-auto mt-5">
  <h5 class="pb-4">
    <span style="background-color:#f5f5f5">
        注文情報入力　
    </span>
    <!-- カートにアイテムが存在しているかを判断　-->
  <% if current_customer.cart_items.present? %>
  </h5>
    <%= form_with model: @order, method: :post, url: new_order_path, method: :post, local: true do |f| %>
      <strong>支払方法</strong><br />
      <%= f.radio_button :pay_type, '0' %>
	   	<%= f.label :pay_type, "クレジットカード", {value: '0', style: "display: inline-block;"} %><br />
	   	<%= f.radio_button :pay_type, '1' %>
	   	<%= f.label :pay_type, "銀行振込", {value: '1', style: "display: inline-block;"} %><br />
	   	
      <strong>お届け先</strong><br />
      <%= f.radio_button :address, "my_addresses" %>
	   	<%= f.label :address, "ご自身の住所", {style: "display: inline-block;"} %><br />
	   	<%= my_address %>
	   	<% if @address != [] %>
	   	  <%= f.radio_button :address, "shipping_addresses" %>
	   	  <%= f.label :address, "登録済み住所から選択", {style: "display: inline-block;"} %><br />
        <%= f.collection_select(:address_id, @address, :id, :order_address) %><br />
      <% end %><br/>
      <%= f.radio_button :address, "new_address" %>  
	   	<%= f.label :address, "新しいお届け先", {style: "display: inline-block;"} %><br />
      <%= f.label :postal_code, "郵便番号（ハイフンなし）" %>
      <%= f.text_field :postal_code, placeholder: "0000000" %><br />
      <%= f.label :address, "住所" %>
      <%= f.text_field :address, placeholder: "東京都渋谷区代々木神園町0-0" %><br />
      <%= f.label :name, "宛名" %>
      <%= f.text_field :name, placeholder: "令和道子" %><br />
      <%= f.submit "確認画面へ進む", class:"btn btn-primary" %>
    <% end %>
    <% else %>
    <!-- カートにアイテムが存在しない場合の処理　-->
      <p>ショッピングカートの中身が空です。</p>
      <p><%= link_to "こちら",items_path %>から商品をカートに追加してください。</p>
    <% end %>
    
    
    
</div>
