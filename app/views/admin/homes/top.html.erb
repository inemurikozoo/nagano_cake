
<div class="col-md-6 mx-auto mt-5">
  <h5 class="head-line text-left">注文履歴一覧</h5>
  <table class="table table-hover ">
  <thead class="thead-light">
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
      </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
    <tr>
      <td>
        <%= link_to admin_path(order.id) %>
      </td>
      <td>
        <%= order.customer.last_name & order.customer.first_name %>
      </td>
      <td>
      </td>
      <td>

      </td>
    </tr>
    <% end %>
  </tbody>
  </table>
</div>
